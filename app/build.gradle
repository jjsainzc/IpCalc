apply plugin: 'com.android.application'
android {
    signingConfigs {
        config {
            storeFile file('C:/Users/alienware/.android/debug.keystore')
        }
    }
    compileSdkVersion 19
    buildToolsVersion "19.1.0"
    defaultConfig {
        applicationId "com.example.ipcalc"
        minSdkVersion 8
        targetSdkVersion 18
    }
}

dependencies {
}